<div class="text-red-500 grid grid-cols-6 gap-4">
    <div class="col-span-4 h-[200px]">

        <% content_for :title, "Posts" %>

        <% @posts.each do |post| %>
            <%= render 'postItem', post: post %>
        <% end %>
    </div>
    <div class="col-span-2 h-[200px] bg-green-300">
    
    </div>
</div>